<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/form.css">
    <title>Taskist</title>
</head>
<body class="d-flex flex-column align-items-center justify-content-center pb-5">
    <h1 class="mb-5">Criar Tarefa</h1>
    <form action="/tasks/add" method="post" class="p-3 col-md-6 col-lg-4 col-xl-3 col-12">
        <% if(errorMessage) { %>
            <p class="text-danger error-message">
                <%= errorMessage %>
            </p>
        <% } %>
        <div class="mb-3">
            <label for="exampleFormControlInput1" class="form-label">Tarefa</label>
            <input type="text" name="title" class="form-control" id="title" placeholder="Digite a tarefa"/>
        </div>
        <div class="mb-3">
            <span class="d-flex align-items-center justify-content-between flex-row">
                <p>Data de finalização</p>
                <p>Amanhã <%= new Date(Date.now() + 24*60*60*1000).toString().substring(16, 21) %></p>
            </span>
        </div>
        <input type="submit" value="Adicionar" class="btn btn-primary w-100">
    </form>
</body>
</html>